# Environment
ENVIRONMENT=development

# Database (using psycopg3 driver)
# Local development (Docker):
DATABASE_URL=postgresql+psycopg://signals_user:signals_password@localhost:5432/trading_signals

# Production (Supabase) - Set this in Vercel dashboard env variables:
# DATABASE_URL=postgresql://postgres.[project-ref]:[password]@aws-1-[region].pooler.supabase.com:5432/postgres
# SUPABASE_URL_SESSION_POOLER=postgresql://postgres.[project-ref]:[password]@aws-1-[region].pooler.supabase.com:5432/postgres

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_RELOAD=True

# CORS Origins (JSON array format for pydantic-settings 2.5+)
CORS_ORIGINS=["http://localhost:3000","https://signals-dvf.vercel.app","https://*.vercel.app"]

# Email Service (Resend)
RESEND_API_KEY=re_xxxxxxxxxxxxx
RESEND_FROM_EMAIL="Signals <onboarding@resend.dev>"
# Get your API key from: https://resend.com/api-keys

# Email Configuration (for confirmation links in emails)
APP_BASE_URL=http://localhost:3000
# Production: Set to https://signals-dvf.vercel.app/
