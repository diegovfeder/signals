# Database (using psycopg3 driver)
DATABASE_URL=postgresql+psycopg://signals_user:signals_password@localhost:5432/trading_signals

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_RELOAD=True

# CORS Origins (JSON array format for pydantic-settings 2.5+)
CORS_ORIGINS=["http://localhost:3000"]

# Email Service (Resend)
RESEND_API_KEY=re_xxxxxxxxxxxxx

# Environment
ENVIRONMENT=development
