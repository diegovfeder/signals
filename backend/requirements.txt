# FastAPI and Web Framework
fastapi>=0.104.0,<0.110.0
uvicorn[standard]>=0.24.0,<0.28.0
python-multipart>=0.0.6

# Database
sqlalchemy>=2.0.35
# Using psycopg instead of asyncpg and psycopg2-binary for Python 3.13 support
psycopg[binary,pool]>=3.1.0

# Data Validation (Python 3.13 compatible)
pydantic>=2.8.0
pydantic-settings>=2.5.0
email-validator>=2.1.0

# Email Service
resend>=0.7.0

# Utilities
python-dotenv>=1.0.0
python-jose[cryptography]>=3.3.0

# CORS - using starlette middleware instead of fastapi-cors
# fastapi-cors>=0.0.6
