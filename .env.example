# =============================================================================
# Trading Signals MVP - Environment Variables
# =============================================================================
# Copy this file: cp .env.example .env
# Then customize values for your environment
#
# For project-specific .env files:
#   Backend:  cd backend && cp .env.example .env
#   Frontend: cd frontend && cp .env.example .env.local
#   Pipeline: cd pipe && cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# BACKEND (FastAPI)
# -----------------------------------------------------------------------------
# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Database connection
# Local dev: postgresql://signals_user:signals_password@localhost:5432/trading_signals
# Production (Supabase pooler): Use port 6543, Session mode
DATABASE_URL=postgresql://signals_user:signals_password@localhost:5432/trading_signals

# Environment flag
ENVIRONMENT=development

# Email service
RESEND_API_KEY=re_xxxxxxxxxxxxx

# -----------------------------------------------------------------------------
# FRONTEND (Next.js)
# -----------------------------------------------------------------------------
# Backend API URL
# Local: http://localhost:8000
# Production: https://signals-api-dvf.vercel.app
NEXT_PUBLIC_API_URL=http://localhost:8000

# Analytics (PostHog - optional)
NEXT_PUBLIC_POSTHOG_KEY=phc_xxxxxxxxxxxxx
NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# -----------------------------------------------------------------------------
# PIPELINE (Prefect)
# -----------------------------------------------------------------------------
# Database connection (same as backend, no +psycopg suffix for Prefect)
# DATABASE_URL (reuse from BACKEND section above)

# Email service (same as backend)
# RESEND_API_KEY (reuse from BACKEND section above)

# Email configuration
RESEND_FROM_EMAIL=signals@yourdomain.com

# Signal notification threshold (0-100)
SIGNAL_NOTIFY_THRESHOLD=70

# Symbols to track (comma-separated)
SIGNAL_SYMBOLS=BTC-USD,AAPL

# Prefect Cloud (optional - if deploying to Prefect Cloud)
PREFECT_API_KEY=pnu_xxxxxxxxxxxxx
PREFECT_WORKSPACE=your-workspace-id

# -----------------------------------------------------------------------------
# ANALYTICS (Optional)
# -----------------------------------------------------------------------------
# PostHog (backend analytics)
POSTHOG_API_KEY=phc_xxxxxxxxxxxxx
